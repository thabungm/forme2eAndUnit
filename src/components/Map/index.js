import GoogleMapReact from 'google-map-react';

const Map = () => {
  return (
    <div>
      {/* <GoogleMapReact
        bootstrapURLKeys={{
          key: '',
          language: 'en',
        }}
        defaultCenter={}
        center
        defaultZoom
        // onChildMouseEnter={()=>}
        // onChildMouseLeave={this.onChildMouseLeave}
      ></GoogleMapReact> */}
    </div>
  );
};

export default Map;
